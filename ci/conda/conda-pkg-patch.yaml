channels:
  - conda-forge
  - dharpa

host_requirements:
  - pip
  - python
  - setuptools<=63
  - setuptools_scm

test:
  imports:
    - kiara_plugin.streamlit
  source_files:
    - tests
    - examples
  commands:
    - kiara module list
